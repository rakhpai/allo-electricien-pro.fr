baseURL = "https://allo-electricien.pro/"
languageCode = "fr-FR"
title = "SOS Électricien Paris | Dépannage Électricité Urgence 24/7 IDF"
enableGitInfo = true  # Enable Git info for lastmod dates in sitemap

[params]
  description = "SOS Électricien Paris & IDF ⚡ Dépannage électricité en urgence <30min. 410 professionnels 24/7. Court-circuit, panne totale, installation. ☎️ 01 44 90 11 31"

  # Company Information
  companyName = "ECLAT RAPIDE ELECTRICITE"
  companyLegalForm = "Société à responsabilité limitée (Société à associé unique)"
  siret = "988 601 175 00017"
  rcs = "988 601 175 R.C.S. Créteil"
  capital = "1 000,00 €"
  address = "55-69 Avenue de Fontainebleau"
  postalCode = "94270"
  city = "Le Kremlin-Bicêtre"
  country = "France"

  # Contact
  mainPhone = "01 44 90 11 31"
  mainPhoneRaw = "0144901131"
  email = "contact@allo-electricien.pro"

  # WhatsApp Contact
  whatsappNumber = "+33 6 20 55 82 97"
  whatsappNumberRaw = "33620558297"
  whatsappMessage = "Bonjour, j'ai besoin d'un électricien en urgence"

  # SEO
  keywords = "sos électricien, sos électricien paris, dépannage électricité paris, électricien urgence, dépannage électrique, panne électricité, électricien 24/7, réparation électrique, installation électrique, urgence électrique paris, électricien urgent paris"

  # Video Configuration
  [params.video]
    # Local video paths (relative to static/)
    basePathLocal = "/videos-downloaded"
    # Thumbnail paths (when available)
    thumbnailPath = "/images/video"
    # Video formats priority (WebM first for better compression)
    preferredFormat = "webm"
    # Enable/disable video player
    enabled = true
    # Preload strategy: "none", "metadata", "auto"
    preload = "metadata"
    # Video player controls
    showControls = true
    playsinline = true
    muted = false
    autoplay = false

[permalinks]
  pages = "/:slug/"

[taxonomies]
  category = "categories"
  tag = "tags"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true  # Allow raw HTML in markdown

[outputs]
  home = ["HTML", "JSON", "RSS", "Sitemap"]
  section = ["HTML", "RSS", "Sitemap"]
  page = ["HTML"]

[minify]
  disableXML = false
  minifyOutput = true

[sitemap]
  changefreq = "monthly"  # More realistic default for most city pages
  filename = "sitemap.xml"
  priority = 0.5  # Default priority for regular pages without explicit priority